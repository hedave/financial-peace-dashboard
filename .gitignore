# Secrets — use config.example.js locally; Netlify builds config from env vars
js/config.js

# Personal budget backups (keep your own copies outside the repo)
backups/

# Bank statements (never commit — may contain account/routing numbers)
*.pdf
*.PDF

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
docs/.Rhistory
.Rhistory
