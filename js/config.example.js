/** Copy this file to config.js and fill in your Supabase values (see DEPLOY.md). */
export const SUPABASE_URL = 'https://YOUR_PROJECT.supabase.co';
/** Supabase dashboard calls this the "publishable" key (same role as the old anon key). */
export const SUPABASE_ANON_KEY = 'sb_publishable_... or eyJ...';
export const CLOUD_SYNC_ENABLED = true;